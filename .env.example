# Local test stack: docker compose up --build -d
# These values are examples, not production credentials.
TENGU_HUB_TOKENS=dev=tengu-local
# Optional: leave empty to run the status tool only.
TENGU_JIRA_BASE_URL=
# docker-compose.yml uses dev-Vault with the dev-root token.
