style:
  ReturnCount:
    # AXI-идиома: ранние guard-выходы с help-хинтами (exit 2 без раундтрипа).
    # Правило продолжает ловить возвраты из глубины тела функции, где ранний
    # выход маскирует ветвление логики.
    excludeGuardClauses: true
